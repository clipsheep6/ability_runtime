asdasdasdasdasdasdasd

aligncenter

居中对齐当前块或图像。

alignjustify

完全对齐当前块或图像。

alignleft

左对齐当前块或图像。

alignnone

移除当前块或图像的对齐方式。

alignright

右对齐当前块或图像。

blockquote

将块引用格式应用于当前块级元素。

backcolor

将背景颜色应用于选择。

blocks

带有块格式的下拉列表以应用于选择。

bold

将粗体格式应用于当前选择。

copy

将当前选择复制到剪贴板。

cut

将当前选择剪切到剪贴板。

fontfamily

带有要应用于选择的字体系列的下拉列表。

fontsize

带有要应用于选择的字体大小的下拉列表。

forecolor

将前景/文本颜色应用于选择。

h1

将当前行更改为“标题 1”样式。

h2

将当前行更改为“标题 2”样式。

h3

将当前行更改为“标题 3”样式。

h4

将当前行更改为“标题 4”样式。

h5

将当前行更改为“标题 5”样式。

h6

将当前行更改为“标题 6”样式。

hr

在编辑器中插入水平线。

indent

缩进当前列表项或块元素。

italic

将斜体格式应用于当前选择。

language

包含要应用于选择的语言的下拉列表。此按钮需要content_langs选项。

lineheight

带有要应用于选择的行高的下拉列表。

newdocument

创建一个新文档。

outdent

超出当前列表项或块元素。

paste

将当前剪贴板粘贴到编辑器中。

pastetext

打开/关闭纯文本粘贴模式。在纯文本模式下，所有富内容都将转换为纯文本。

print

打印当前编辑器内容。

redo

重做上次撤消的操作。

remove

移除（删除）选中的内容或光标位置之前的内容。

removeformat

从当前选择中删除格式。

selectall

选择编辑器中的所有内容。

strikethrough

将删除线格式应用于当前选择。

styles

下拉列表，其中包含要应用于选择的样式。

subscript

将下标格式应用于当前选择。

superscript

将上标格式应用于当前选择。

underline

将下划线格式应用于当前选择。

undo

撤消上一次操作。

visualaid